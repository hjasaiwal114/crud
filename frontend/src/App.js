import { useState } from "react";

function App() {
   const [notes, setNotes] = useState(null);

   const fetchNotes = () => {
    //  Fetch the notes
    //  Set to state
   }

  return (
    <div className="App">
      kowtigh
    </div>
  );
}

export default App;
